# Packages to install in your conda env

`conda install pip`     
`pip install tensorflow`    
`pip install tensorflow_datasets`       
`pip install matplotlib`        
`pip install jupyter`   
`conda install cython`  
`conda install protobuf`    
`pip install pycocotools`   
